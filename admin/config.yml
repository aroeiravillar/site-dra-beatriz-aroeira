# =============================================================
# Painel Admin — Sveltia CMS (Dra. Beatriz Aroeira)
# Funciona em Vercel + GitHub via OAuth (auth.sveltia.dev)
# Repositorio: github.com/aroeiravillar/site-dra-beatriz-aroeira
# =============================================================
backend:
  name: github
  repo: aroeiravillar/site-dra-beatriz-aroeira
  branch: main

# Pasta onde as imagens dos artigos sao salvas
media_folder: "assets/blog"
public_folder: "/assets/blog"

# Idioma do painel
locale: "pt"

# Permite preview antes de publicar
publish_mode: simple

# =============================================================
# Colecoes editaveis pelo painel
# =============================================================
collections:
  - name: "blog"
    label: "Artigos do Blog"
    label_singular: "Artigo"
    folder: "blog/posts"
    create: true
    slug: "{{slug}}"
    extension: "md"
    format: "frontmatter"
    fields:
      - { label: "Título", name: "title", widget: "string" }
      - { label: "Subtítulo / Resumo", name: "subtitle", widget: "text", required: false }
      - { label: "Data de publicação", name: "date", widget: "datetime", default: "" }
      - { label: "Autor", name: "author", widget: "string", default: "Dra. Beatriz Aroeira" }
      - { label: "Categoria", name: "category", widget: "select", options: ["Contracepção", "Sexualidade", "Gravidez", "Saúde Íntima", "Ginecologia", "Estética Íntima", "Menopausa"], default: "Saúde Íntima" }
      - { label: "Imagem de capa", name: "image", widget: "image" }
      - { label: "Texto alternativo da imagem", name: "image_alt", widget: "string" }
      - { label: "Tempo de leitura (minutos)", name: "reading_time", widget: "number", default: 5, min: 1 }
      - { label: "Palavras-chave (SEO)", name: "keywords", widget: "string", required: false, hint: "Separe por vírgulas. Ex: ginecologista BH, sexualidade, contracepção" }
      - { label: "Meta descrição (SEO)", name: "description", widget: "text", required: false, hint: "Até 160 caracteres" }
      - { label: "Conteúdo", name: "body", widget: "markdown" }
